server:
  port: 8082
spring:
  application:
    name: clientes
  cloud:
    config:
      uri: http://localhost:8888

---
server:
  port: 8082
spring:
  profiles: docker
  application:
    name: clientes
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true